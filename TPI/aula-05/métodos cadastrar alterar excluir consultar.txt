 public void cadastrar(){
        String sql;
        sql= "Insert into usuarios(codigo,login,senha,telefone)values"+ "( "
                + "" + getCodigo()+ " ,'" + getLogin()+ "' ,'" + getSenha()+ "', '" + getTelefone()+ "')";
        con.executeSQL(sql);
        JOptionPane.showMessageDialog(null, "Gravado com Sucesso...");              
    }
    
    
     public void excluir(){
        String sql;
        sql = "Delete FROM usuarios WHERE codigo=" +getCodigo()+ "";
        con.executeSQL(sql);
        JOptionPane.showMessageDialog(null, "Registro Excluido com sucesso...");
        
       }
    public void Alterar(){
        String sql;
        sql = "UPDATE usuarios set login='" + getLogin()+   "' ,telefone= '" + getTelefone()+ "' ,senha= '" + getSenha() + "' WHERE codigo='" +this.getCodigo()+"' ";
        
        con.executeSQL(sql);
        JOptionPane.showMessageDialog(null, "Registro Alterado com sucesso...");
    }
    
    public ResultSet consultar()
    {
        ResultSet tabela;
        tabela= null;
        
        String sql= "Select * from usuarios";
        tabela= con.RetornarResultset(sql);
        return tabela;
    }